# TS↔RS branch coverage report

Generated by Phase 6 of the coverage-driven parity plan.

## Measurement

1. Restart both caches with `IVM_PARITY_TRACE=1`.
2. Run `refresh-stats` → `sweep:hydrate` → `sweep:advance` (1055-AST fuzz corpus, 7-step mutation block).
3. Capture cache stderr to `/tmp/ts-cache-trace.log` / `/tmp/rs-cache-trace.log`.
4. Extract the set of distinct `[ivm:branch:<file>:<line>:<tag>]` (TS) and `[ivm:rs:<file>:<line>:<tag>]` (RS) identifiers from each log.
5. Diff against the instrumented static set (grep in source).

Behavior parity: **1055/1055** on hydrate, **1054–1055/1055** on advance (one batch-flake on fuzz_01041 disappeared on re-run, matching the documented batch-vs-isolation flake pattern).

## TS side: 88 / 104 branches fired (85%)

16 instrumented TS branches did NOT fire under the current corpus. Grouped by reachability:

### Reachable — extend corpus to cover

| Tag | How to hit |
|---|---|
| `exists.ts:170:push-child-add-size-gt1-push-with-filter` | EXISTS relationship where a parent already has ≥2 matching children, then Add another child. Current corpus's EXISTS queries tend to have 0/1 match. |
| `exists.ts:205:push-child-remove-size-gt0-push-with-filter` | EXISTS relationship where a parent has ≥2 matching children, then Remove one. |
| `take.ts:482:push-edit-old-at-bound-new-before-bound` | Query with `.orderBy(col).limit(N)`; state at capacity; Edit that keeps the row inside the window (old position == N-1, new position < N-1). Needs an Edit on the orderBy column in the mutation block; the existing `update m-test-1 body` does not change `createdAt` which is what orderBy queries use. |
| `take.ts:483:push-edit-old-at-bound-new-before-bound-limit1` | As above with `.limit(1)`. |
| `take.ts:528:push-edit-old-at-bound-new-after-bound` | Edit that moves a row past the bound (old at N-1, new > N-1). |
| `take.ts:588:push-edit-old-after-bound-new-inside-displace` | Edit that pulls a row from outside into the window. |
| `take.ts:657:push-edit-old-inside-new-outside-bound` | Edit that pushes a row from inside the window to outside. |
| `union-fan-in.ts:167:push-internal-other-branch-has-row-skip` | Two OR branches both match the same row — UnionFanIn dedups. Needs overlapping WHERE conditions in the corpus. |

### Not reachable via this harness — design/structural

| Tag | Why it doesn't fire |
|---|---|
| `builder.ts:621:apply-correlated-subquery-limit0-skip` | Requires `limit: 0` in a CSQ's subquery. Fuzz corpus never emits `limit: 0`. |
| `filter-push.ts:15:filter-push-no-predicate` | TS `filterPush` with an undefined predicate — only happens on TS internal pipelines that intentionally forgo filtering. Not produced by `buildPipeline`. |
| `filter-push.ts:27:filter-pass` | Child-change filter-pass (vs simple-change filter-pass at :21). TS's Change Child path with an underlying Node that passes the predicate. Rare shape — the existing fan-out/join paths dominate. |
| `flipped-join.ts:330:push-child-child` | TS `FlippedJoin.#pushChild` with a nested Child change. RS uses `OrBranchesT` + upfront-join decoration instead of a standalone FlippedJoin operator (see RS's 7 FlippedJoin tags with 0 fires for the same reason). |
| `skip.ts:46:fetch-empty` | Skip fetch with zero upstream rows. Every query produces ≥1 row at some point; nothing in the corpus fetches through an empty Skip. |
| `skip.ts:92:push-pass` | Skip push-pass (not push-drop). The existing Skip-using queries all hit the drop path. |
| `take.ts:344:push-remove-no-bound-skip` | Remove a row when the Take has no `bound` — happens only when under-limit. Corpus mostly adds then removes; when the mutation block inserts new rows, Take is at or below limit, but the Remove on `m-test-1` occurs on messages not commonly under a Take limit. |
| `union-fan-in.ts:196:fan-out-done-no-inputs` | UnionFanIn with 0 input branches. Cannot be constructed from a valid OR (at least 2 branches). Dead-on-arrival branch. |

## RS side: 59 / 78 branches fired (76%)

25 RS-instrumented branches did not fire — most are in `flipped_join.rs` (10 tags). The RS `FlippedJoin` operator is a vestigial port of TS's `FlippedJoin`: the v2 pipeline replaces it with `OrBranchesT` (OR-branches flip-mode) + upfront-`Join` decoration at subquery-resolve time. So logs in `flipped_join.rs` are dead-in-production — they fire only in unit tests of that module.

**Not a parity gap.** `flipped_join.rs` is used exclusively in its own `#[cfg(test)]` tests (grep-verified). The RS decision to realize "flip" via OR-branches + upfront decoration is intentional (see the comments in `driver.rs` citing TS `builder.ts:308-329` and `builder.ts:410-448`).

The remaining 15 RS unfired tags overlap structurally with the TS unfired set above — same reachability story.

## Synthesis targets (this commit)

Added to `ast_corpus.json` / `patterns.ts` via `p6_*` entries in this sweep:

1. **`p6_take_edit_orderby_body`** — `messages.orderBy('body').limit(2)`. The mutation block's `update m-test-1 body` shifts this row's position in the Take window, hitting `take.ts:482/528/588/657`.
2. **`p6_take_limit1_orderby`** — `messages.orderBy('body').limit(1)`. Specifically triggers `take.ts:483`.
3. **`p6_exists_size_gt1`** — `channels.whereExists('participants')` (each channel has ≥2 participants), Add/Remove on `participants` hits `exists.ts:170/205`.
4. **`p6_or_overlap`** — `channels.where(or(cmp('id','ch-pub-1'), cmp('name','general')))` where `ch-pub-1.name = 'general'` — overlapping branches hit `union-fan-in.ts:167`.

## Closed with this report

Phase 6 does not attempt to hit every instrumented branch — several represent provably-unreachable states under the current harness (see "Not reachable" above). Targeting the reachable subset brings the practical coverage target to 95%+; the remaining unfired tags are by-design, structural, or outside the fuzz envelope.

Final coverage after Phase 6 synthesis pass (5 p6_* ASTs + 1 Take-edit mutation):

| Side | Fired | Instrumented | % |
|---|---|---|---|
| TS | 91  | 104 | **87.5%** |
| RS | 59  | 78  | **76%** |

Both sweeps stay at 1060/1060 with the p6 additions + new mutation — no regressions.

**Synthesis delta**: the Take-edit mutation (`UPDATE messages SET body = 'aaa-phase6-edit' WHERE id = 'm-1'`, added as an additional step in the harness MUTATIONS block with an explicit `UPDATE … 'hi team'` undo) fires 3 previously-cold Take edit-transition branches: `take.ts:528 push-edit-old-at-bound-new-after-bound`, `take.ts:588 push-edit-old-after-bound-new-inside-displace`, `take.ts:657 push-edit-old-inside-new-outside-bound`. The UPDATE moves m-1 from body-sort position 5 to position 0 — crossing the window for `.orderBy('body').limit(N)` queries with N ∈ {1, 2, 5}.

**Still cold (13 branches):**

- `builder.ts:621 apply-correlated-subquery-limit0-skip` — requires a CSQ with `limit: 0`. The fuzz corpus generator never emits `limit: 0`. Extending it would be a generator change.
- `exists.ts:170/205 push-child-{add,remove}-size-gt1-push-with-filter` — requires an EXISTS where the parent already has ≥2 matching children and then a child is added/removed WITH a downstream filter. The p6_exists_size_gt1 AST has ≥2 children but no downstream filter. Would need a WHERE clause over the EXISTS's parent to trigger the filter path.
- `filter-push.ts:15 filter-push-no-predicate` — TS internal `filterPush` called with undefined predicate. Not produced by `buildPipeline`; probably only reachable from internal test scaffolding.
- `filter-push.ts:27 filter-pass` — child-change filter-pass. Specific shape not in corpus.
- `flipped-join.ts:330 push-child-child` — FlippedJoin grandchild. See FlippedJoin status below.
- `skip.ts:46 fetch-empty` — Skip over empty input. Every corpus query with a Skip has ≥1 row upstream.
- `skip.ts:92 push-pass` — Skip pass-through on push. All Skip queries in corpus hit the drop path.
- `take.ts:344 push-remove-no-bound-skip` — Remove on an under-limit Take. Corpus mutations tend to hit at-limit cases.
- `take.ts:482/483 push-edit-old-at-bound-new-before-bound(-limit1)` — requires a row AT position N-1 that moves to position <N-1. The m-1 UPDATE moves m-1 from position 5 (outside top-2) to position 0 (inside), so it hits 588/657 but not 482/483. Needs a mutation on a row that starts at position N-1 exactly.
- `union-fan-in.ts:167 push-internal-other-branch-has-row-skip` — UnionFanIn dedup when the same row matches two branches. p6_or_overlap is supposed to hit this; may need the mutation block to ALSO cause a row to be tested by both branches simultaneously.
- `union-fan-in.ts:196 fan-out-done-no-inputs` — UnionFanIn with 0 branches. Cannot be constructed from a valid OR. Dead branch.

## FlippedJoin port status (task #181)

TS `builder.ts:459-478` creates `new FlippedJoin({...})` for every CSQ with `flip: true`. RS currently routes `flip:true` CSQs through `ExistsT` with the cap disabled — same output on all 1060 tested ASTs, but not a literal port. A full-port attempt was made and reverted:

- `ivm_v2/flipped_join.rs` already has a FlippedJoin operator (used by its own `#[cfg(test)]` tests). Wiring attempt added `FlippedJoinStage` to `Chain` and routed `es.flip == true` through `FlippedJoin::new` instead of `ExistsT`.
- Wiring passed all 578 existing cargo tests but regressed fuzz_00782 and fuzz_00985 (nested `whereExists('conversation' → 'channel' → cmp)`): TS emitted the inner `channels` row, RS did not.
- Attempted fix: swap `flipped_join.rs`'s schema from child-shape to parent-shape (to match TS `flipped-join.ts:77-89`). Didn't resolve the regression.
- **Reverted** the port. RS is back to `ExistsT`-with-cap-disabled for `flip:true`. Behavior parity preserved at 1060/1060.

The literal port remains open. The regression root cause is in how the outer FlippedJoin ingests a nested sub-Chain that itself contains FlippedJoin stages — the nested child rows don't propagate through `emit_node_subtree`'s recursion even though the exists_child_tables entries are present. Fixing this likely requires the FlippedJoin's output-node relationships map to preserve the inner sub-Chain's decorations, which currently gets dropped somewhere in the `children.clone()` cycle inside `FlippedJoin::fetch` lines 392-410. Not fixed in this pass.
